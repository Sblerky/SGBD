tp1
Se co à une machine locale

Se co sur Butor : ssh -X login@butor 
mdp: pareil que la session
(Faire régulièrement commit ou rollback sinon on casse la fac)


Récup oraenv.sh sur /export/home/oracle le copier dans répertoire de travail

ORACLE_HOME=/oracle/app/oracle/product/11.2.0/db_home1;

ORACLE_SIDE=ENSB2019;

PATH=$PATH:$ORACLE_HOME/bin:/opt/csw/bin;


Executer le .sh :". ./oraenv.sh"


Lancer sql+ : rlwrap sqlplus

login=mdp


commandes sqlplus :

help <commande>

set pause on (page à page)

set pagesize (taille de la page)

set linesize (taille de la ligne)

set serverout on (utiliser DBMS_OUTPUT.OUT_LINE())

col <nom_colonne> format <format>
(à mettre dans login.sql)



Table étudiant :

noEtu NUMBER(6,0)

nomEtu VARCHAR(50)

villeEtu VARCHAR(50)

genre(H/F) CHAR(1)

statutM(C,P,D,V,M) 
CHAR(1)
desc 
VARCHAR(2000)



Paris 22% 	Chalon 12% 	Lyon 20% 	Longvic 10% 	Dijon 16%
Quetigny 8% 	Chenove 6%	Chevigny 4%	Bressey 2%	Mirande 1 péqueneau



DBMS_RANDOM.string('L',TRUNC(DBMS_RANDOM.value(10,21)))



------------------------------------------

tp2
ecrire requete pour connaitre le pourcentage d'étudiant par ville


------------------------------------------

tp3
Test sur index primaire


------------------------------------------

tp4

ANALYZE INDEX <nomIndex> VALIDATE STRUCTURE	-> génère 1 ligne dans la table INDEX_STATS
	
HEIGHT
	BLOCKS 		-> nombre blocs alloués
	
LF_ROWS		-> nombre de valeur dans les feuilles
	
LF_BLKS		-> nombre de blocs "feuilles"
	
BR_BLKS 	-> nombre de blocs "branches"
	
DEL_LF_ROWS 	
	DISTINCT_KEYS
	BTREE_SPACE
	USED_SPACE



Comparé la taille d'un index secondaire avec un index bitmap sur noEtu, 
sur ville, sur nom.
Comparé la taille d'une table hors clustereiexit
 avec la taille de la même table dans un cluster.





















